<!-- REGION : Load Active Tab -->

<% active_tab=[] %>

<%
 case current_tab

  when 1
    active_tab.fill("")
    active_tab[1] = "active"
    
  when 2
    active_tab.fill("")
    active_tab[2] = "active"
    
  else

  active_tab.fill("")

  end
%>

<!-- END REGION : Load Active Tab -->

    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
 		<a class="navbar-brand" href="#">
 			<%= image_tag "Logo.png", :alt => "Ruby Logo", :title => "Ruby", :style => "height: 80px" %>
 		</a>
 		<button class="navbar-toggle" type="button" data-toggle="collapse" data-target="#navbar-main">
			<span class="sr-only">Toggle navigation</span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
		</button>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse mar-top-30" id="navbar-main">
		<ul class="nav navbar-nav Menu">
			<li class="<%= active_tab[1] %>">
				<a href="<%= url_for(:controller => "shop", :action => "index") %>">Shops 
				<span class="sr-only">(current)</span></a>
			</li>
			<li class="<%= active_tab[2] %>">
				<a href="<%= url_for(:controller => "shop", :action => "new") %>">Register New 
				<span class="sr-only">(current)</span></a>
			</li>
  		</ul>
    </div><!-- /.navbar-collapse -->
